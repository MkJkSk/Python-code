def greet(fname,Lname):
    return(f"Hi {fname} {Lname}")
def First(father_name,Mname):
    return father_name + Mname

Result = greet("Guido"," Van ") 
sol = First(" Rossum " ," he is scientist ")

print(Result + sol) 
